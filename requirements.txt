# ============================================================
# requirements.txt
# ABSA balé by BTN — IndoBERT + E-S-QUAL Framework
# Python 3.9+ | CUDA 11.8+ (opsional, untuk GPU training)
# ============================================================

# ── Data Collection ─────────────────────────────────────────
google-play-scraper>=1.2.4

# ── Data Processing ─────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# ── Text Preprocessing ──────────────────────────────────────
PySastrawi>=1.0.0            # Stemming bahasa Indonesia
emoji>=2.8.0                 # Penghapusan emoji
langdetect>=1.0.9            # Deteksi bahasa
regex>=2023.6.3

# ── NLP & Transformers ──────────────────────────────────────
transformers>=4.35.0         # IndoBERT, tokenizer
datasets>=2.14.0             # Dataset utilities
tokenizers>=0.15.0
sentence-transformers>=2.2.2 # Embedding untuk klasterisasi

# ── Deep Learning ───────────────────────────────────────────
torch>=2.0.0                 # PyTorch (CPU)
# torch>=2.0.0+cu118         # PyTorch dengan CUDA 11.8 (uncomment jika GPU)

# ── Machine Learning ────────────────────────────────────────
scikit-learn>=1.3.0
scipy>=1.11.0
imbalanced-learn>=0.11.0     # SMOTE untuk class imbalance

# ── Visualisasi ─────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
wordcloud>=1.9.2

# ── Utilitas ────────────────────────────────────────────────
accelerate>=0.24.0           # Optimasi training (opsional)


# ============================================================
# CATATAN INSTALASI
# ============================================================
# 1. Buat virtual environment:
#    python -m venv venv
#    source venv/bin/activate  # Linux/Mac
#    venv\Scripts\activate     # Windows
#
# 2. Install dependencies:
#    pip install -r requirements.txt
#
# 3. Untuk GPU training, ganti baris torch di atas dengan versi CUDA:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 4. Download model IndoBERT (otomatis saat pertama kali dijalankan):
#    - indolem/indobert-base-uncased
#    - indolem/indobertweet-base-uncased  (direkomendasikan)
# ============================================================
